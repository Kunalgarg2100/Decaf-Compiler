class Program {

	int inp[110];

	void main() {
		int n;
		n = 90;
		initialize(n);
		quicksort(0,n-1);
		for i=0, 10{
			callout("printf","%d ",inp[i]);
		}
		callout("printf","\n");
	}

	void initialize(int n){
		int i;
		for i=0, n-1 {
			inp[i] = n - i;
		}
		for i=0, 10{
			callout("printf","%d ",inp[i]);
		}
		callout("printf","\n");
	}

	void quicksort(int s, int e) {	
    	if(s >= e) {
        	return;
    	}
    	else {
    		int p;
    		p = partition(s,e);
    		quicksort(s, p-1);
    		quicksort(p+1,e);
    		return;
    	}
	}

	int partition(int s, int e){
		int pivot;
		pivot = inp[e];
		for i=s, e{
			if(inp[s] < pivot){
				s = s + 1;
			}
			else {
				if(inp[e] > pivot){
					e = e - 1;
				}
				else{
					if(inp[s] == inp[e]){
						s = s + 1;
					}
					else{
						if(s < e){
							int tmp;
							tmp = inp[s];
							inp[s] = inp[e];
							inp[e] = tmp;
						}
					}
				}
			}
		}
		return e;
	}
	
}